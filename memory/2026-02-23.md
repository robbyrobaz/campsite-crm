# 2026-02-23 — Daily Log

## Numerai
- All 3 models submitted successfully (round 1209): robbyrobml, robbyrob2, robbyrob3
- robbyrob2 pipeline reuses robbyrobml's models (MMC orthogonalization) — created symlink for `models_elite_robbyrob2/`
- robbyrob3 trained separately by builder (XGBoost+RF+NN)
- Local validation: robbyrobml val_corr=0.0294 (up from 0.0278), Sharpe=1.90 (up from 1.76)
- Daily bot timer fires at 6:30 AM MST
- Rob decided: stay local (don't push pickles to Numerai) while actively improving models

## Blofin Pipeline — MAJOR Audit & Fixes
- **Pipeline audit completed** → `PIPELINE_AUDIT.md` written
- Pipeline NOT generating alpha: 84,636 closed trades, 37% WR, -0.15% avg PnL
- Only 3 strategies with real FT signal: vwap_reversion (+8.85% SOL), cross_asset_correlation (+127% compounded), bb_squeeze_v2 (+83% compounded)
- Root cause: BT gate was inverted (blocking winners, promoting losers), FT re-promotion trap permanently stuck strategies
- **Fixes applied:**
  - Extended BACKTEST_DAYS 7→30
  - Raised gates: MIN_TRADES 50→100, MIN_PF 1.2→1.35
  - Added FT winner override (200+ trades with positive PnL bypasses BT gate)
  - Added FT block reset (14-day expiry on re-promotion trap)
  - Wired Phase 1b: optimizer results auto-feed into registry
  - Phase 4b registry sync wired into pipeline
  - Archived proven losers: ensemble_top3, ml_gbt_5m, vol_compression_fade, vol_compression_fade_v2
  - Fixed ft_started filter bug (Phase 4 was counting ALL historical trades, not since promotion)
- promote_to_ft.py ran for vwap_reversion, bb_squeeze_v2, cross_asset_correlation → all T2

## NQ Pipeline — Phase 2 Progress
- Agent team (Phase 1 overnight) built: tournament.py, experiment_engine updates, 3 new strategies (momentum, gap_fill, vol_contraction), 238 tests passing
- Phase 2 training re-run with feature fix — strategies now produce realistic trade counts:
  - prev_day: PF 1.04 (best), vwap_fade: PF 1.03, orb: PF 0.83
  - None passing Lucid gate (PF ≥ 1.5) yet
- NQ Dashboard built and live at http://127.0.0.1:8891 (systemd: nq-dashboard.service)
- NQ section added to master dashboard at :8890
- Equal Tops/Bottoms strategy built: 3,701 trades, 28.9% WR standalone — no edge alone, but card created for ML feature engineering to find profitable subset
- **IMPORTANT:** `main` branch is LIVE on Rob's Windows PC with NinjaTrader. All pipeline work on `pipeline/session-trading` branch. Do NOT merge without coordinating. See IMPORTANT.md in repo.

## Master Dashboard Fixes
- Fixed Live Agent Work panel: JS syntax error from Jinja eating `\'` escapes. Used `&quot;` instead.
- Fixed Numerai model stats: same `\'tnum\'` escaping bug. All 3 models now rendering.
- Updated QA checklist: mandatory playwright smoke test + `node --check` for all Flask/Jinja dashboards
- Added to CHECKLIST.md: functional smoke test is NON-OPTIONAL for UI work

## Heartbeat Improvements
- Added Numerai model dir checks to HEARTBEAT.md (all 3 dirs must exist)
- Heartbeat must now investigate WHY services fail, not just report "failed, will retry"

## Infrastructure
- Playwright installed for headless browser testing (QA smoke tests)
- campsite-crm still has no git repo (flagged, not urgent)
- master-dashboard has git but no remote (flagged)
- Token window resets: 7 AM and noon MST (5h windows)

## NQ Pipeline Phase 2 — Signal Bug Fixed & Dashboard Live (2:21 PM)
- **Root cause**: Two compounding bugs — `_compute_fold_metrics` counted ALL OOS bars as trades; no per-strategy candidate filtering
- **Fix**: Added `threshold=0.5` to walk_forward.py (only `ml_score >= 0.5` = trade); added `apply_strategy_candidate_filter()` in run_phase2.py with 6 distinct vectorized filters
- **Result**: ORB now produces 181 OOS trades across 5 folds (down from 21,000+)
- **Dashboard**: Flask app on port 8891, systemd `nq-dashboard.service` enabled+active, 6 panels (strategies, WF results, tournament, sweeps, sessions, model inventory)
- **DB gate_badge bug**: DB stores lowercase "pass"/"fail" but code expected uppercase — fixed with case-insensitive matching
- **Next**: Re-run full Phase 2 pipeline with fixes to regenerate all results; verify all 6 strategies produce 50-500 trades

## BloFin Pipeline Updates
- Phase connections fixed (commit 30a52da): BT window 7→30 days, min trades 50→100, PF 1.2→1.35
- FT winner override: ≥200 trades + positive PnL bypasses BT gate (14-day expiry on FT blocks)
- Force-promoted vwap_reversion, bb_squeeze_v2, cross_asset_correlation to T2/FT
- 4 optimizer strategies registered as T0: atr_breakout_1_5, bb_mean_reversion, stochastic_oscillator, vwap_deviation
- cross_asset_correlation at 140/200 FT trades for auto-promotion

## Numerai
- Round 1209 submitted all 3 models successfully
- Race condition identified: robbyrob2 deletes v5.1/live.parquet mid-run when robbyrobml is running — needs file locking or sequential execution
- robbyrob2 symlink: models_elite_robbyrob2 → models_elite (reuses robbyrobml models + MMC orthogonalization)

## Master Dashboard
- Jinja/JS escaping: Always use `&quot;` not `\'` in JS strings inside Jinja templates — recurring bug, fixed twice today
- `node --check` added to QA process for Jinja dashboards
