
## Model Routing Change — Sonnet for Everything (Mar 5 2026)
- **Haiku is OUT for builders** — incomplete work, wastes time
- **Sonnet is now standard** for ALL builders, kanban runners, coding crons, and main session
- **Haiku retained ONLY for**: Hourly Oversight Check + AI Token Usage Audit (basic non-code crons)
- **Codex is BANNED** — out of credits, never re-enable
- Updated: AGENTS.md, BOOTSTRAP.md, cron/jobs.json (9 crons), kanban settings
- Kanban providerModelConfig: `claude-sonnet-4-6` (no anthropic/ prefix for CLI)

## IBKR NQ Tick Feed — LIVE (Mar 5 2026 morning)
- **Repo**: `github.com/robbyrobaz/ibkr-pipeline` — all committed and pushed
- **IB Gateway**: Docker container (`ib-gateway`, `restart=unless-stopped`), port 4002, paper DUH860616
- **Key rule**: `TWOFA_TIMEOUT_ACTION=exit` + `RELOGIN_AFTER_TWOFA_TIMEOUT=no` — ONE auth attempt only, never auto-retry. Phone app must be CLOSED while feed runs (competing session kills data)
- **Feed service**: `ibkr-nq-feed.service` — NQH6, tick + bid/ask + L2 depth, DuckDB at `ibkr/data/nq_feed.duckdb`
- **Stats by 9 PM**: 1.18M trade ticks, 21.2M quote ticks recorded today
- **1-min bars**: Written to `ibkr/data/NQ_ibkr_1min.csv` (UTC ISO format)
- **NQ pipeline swapped**: `smb_watcher.py` now reads `NQ_ibkr_1min.csv` instead of NinjaTrader SMB CSV
- **Alerts**: ntfy.sh topic `ibkr-nq-feed` fires on disconnect/competing session (10189, 10197, 1100)
- **DuckDB concurrent reads**: Exclusive lock while feed runs — for backtesting, stop feed → query → restart
- **Competing session root cause**: IBKR paper account blocks tick data if live account active on another IP. Phone app was the culprit today.

## Model Routing Change — Sonnet for All Builders (Mar 5 2026)
- Sonnet = everything (main session + all builders + kanban runners)
- Haiku = oversight/token audit crons ONLY
- Codex = BANNED (out of credits)
- Updated: AGENTS.md, BOOTSTRAP.md, cron/jobs.json (9 crons), kanban settings

## NQ Strategy Registry Status (Mar 5 2026 PM)
- **gate_status='pass', live_enabled=0** (should be enabled — Rob's 8:10 AM request):
  - equal_tops_bottoms PF=3.02 (ETB — #1 priority, still not live)
  - orb PF=3.006
  - prev_day PF=2.17
  - vwap_fade PF=1.96 (caution: bleeding live, 14% WR)
  - psych_levels PF=1.92
- **gate_status='fail', live_enabled=1** (failing gate but already live — review why failing):
  - zscore_vwap_mr PF=8.74 (WHY failing? High PF)
  - eight_am_break_retest PF=5.14
  - opening_momentum_bias PF=3.27
- Rob wants to lower BT gate threshold to get more strategies into FT — need to implement

## Moonshot v2 UNIQUE Constraint Bug (Mar 5 2026)
- Every cycle crashes at end: `UNIQUE constraint failed: positions.symbol, positions.direction`
- Cycles 27-30+ all affected. Fix card dispatched to builder (c_739e055d4c90d).
- Workaround: manual restart each heartbeat. Still running, just exits with code 1.

## IBKR L2 Depth — Counter Bug Fixed (Mar 5 2026 evening)
- `depth_today` counter was missing from `nq_feed_status.json` (never tracked)
- Fix: added `self.depth_today = 0`, increment in `_on_depth()`, expose in `_write_status()`
- Also added `last_tick_time` and `last_bar_time` to status JSON
- L2 data WAS flowing fine — 111,417 depth rows in DuckDB by 11 PM
- Commit: `3117946` in ibkr repo

## IBKR Feed Storage — Not a Concern (Mar 5 2026)
- DuckDB actual size: ~74 MB/day (111MB after ~1.5 days)
- Projected: ~1.6 GB/month, ~18 GB/year
- 236 GB free → ~13 years at current rate
- Quotes dominate storage (95%) but DuckDB compresses well (~5 bytes/quote)

## NQ ORB Backtest Suite — 3 Variants (Mar 5 2026 evening)
- Script: `NQ-Trading-PIPELINE/scripts/backtest_ny_orb.py`
- Results: `scripts/orb_backtest_results.md` — commit `85c129a`, pushed to GitHub
- Data: 14 months NQ_continuous_1min.csv, NY open 5-min ORB (14:30-14:35 UTC)
- **V1 (20-tick TP / 200-tick SL, 4 contracts)**: 276 trades, 92% WR, PF 1.155, +$13,600 total
- **V2 (Anticipatory 0.5pt from edge, 4ct)**: Only 9 triggers, PF 0.800, -$800 — too rare
- **V3 (20% range TP / 100% range SL, 1ct)**: 276 trades, 83% WR, PF 0.997, breakeven
- V1 most interesting: 92% WR real but rare losers (-$4K each) drag PF to 1.155

## ORB Prop Firm Reframe — Lucid 50K (Mar 5 2026 late)
- Key insight: prop firm = blowing account costs ~$100 (eval fee), NOT the full drawdown
- Tradovate DLL caps daily losses at platform level (set to $1,250 for Lucid 50K)
- Lucid 50K rules: profit_target=$3,000, trailing_DD=$2,500, DLL=$1,250, min_days=5, consistency=40%
- EV formula: (pass_rate × $1,000 payout) - (bust_rate × $100 eval cost)
- Prop sim card dispatched: `c_c893550a65d91` — using existing `pipeline/prop_sim.py` LucidPropSim
- Results file will be: `scripts/orb_prop_sim_results.md`
- Sensitivity table: DLL $500-$2,000 × contracts 1-6 → find optimal combo for max EV

## L2 Order Flow Research — Complete (Mar 5 2026)
- Script: `/home/rob/infrastructure/ibkr/scripts/l2_research_today.py`
- Results: `scripts/l2_research_results.md` (7.9 KB)
- Builder correctly stopped feed → queried DuckDB → restarted feed at 23:15
- 6 research questions: bid/ask imbalance, large order detection, book thinning, absorption, execution delay analysis, time-of-day patterns
- Rob wants 1-contract entries exploiting L2 patterns with 10-100ms execution delay (→ TradersPost → Tradovate)

## ORB Builder Timing Bug (Mar 5 2026)
- Kanban runner marks long-running cards as Failed prematurely (within first minute)
- Cards are actually still running — check PID before re-queuing
- Pattern: log shows stream events still flowing but status.json says Failed
- Fix: PATCH card back to In Progress, let it complete
