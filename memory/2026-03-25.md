# 2026-03-25 Daily Memory

## Token Usage OAuth Refresh — PERMANENT FIX IMPLEMENTED

**Root cause identified:** OAuth token in `~/.claude/.credentials.json` expired Mar 24 at 1:53 PM. The `token_usage_aggregator.py` script had no refresh logic — it relied on the gateway to refresh tokens, but the gateway uses a separate token from `auth-profiles.json` that lacks the `user:profile` scope needed for the usage API.

**Permanent fix added (Mar 25 05:48):**
- Updated `/home/rob/.openclaw/workspace/brain/scripts/token_usage_aggregator.py` with full OAuth2 refresh flow
- Function `_refresh_oauth_token()` now detects expired tokens (5min buffer) and refreshes via `POST https://platform.claude.com/v1/oauth/token`
- Uses refresh_token from credentials.json, updates file with new access_token + expiry timestamp
- Client ID: `9d1c250a-e61b-44d9-88ed-5944d1962f5e`

**Testing incident:** Aggressive debugging (multiple rapid curl attempts) triggered a 24h rate limit on the token refresh endpoint. Rate limit expected to clear around Mar 25 1:45 PM MST. The timer-based script (`token-usage-tracker.timer`, runs every 15min) will auto-recover once rate limit clears.

**Lesson:** OAuth token refresh endpoints have strict rate limits. Test conservatively.

**Status:** Fix validated and deployed. Once rate limit clears, fresh usage percentages will auto-update every 15min via systemd timer.

---

## Server Health Summary

**Typical operating profile observed:**
- CPU temp: 80-91°C during Moonshot cycles (300-550% CPU on ML training), 75-80°C baseline
- Disk: stable at 68% (299GB used, 145GB free on 468GB nvme)
- RAM: 20-22GB free typical, 21-22GB during normal operation
- Moonshot v2 cycles: run 1-3 hours, RSS up to 2.6GB, exit cleanly
- Blofin ingestor: steady 24% CPU, 450MB RSS (WebSocket + DB writes)
- Blofin paper_engine: steady 30% CPU, 415MB RSS

**Active services (all healthy):**
- openclaw-gateway.service
- openclaw-backup.timer (hourly DB snapshots)
- openclaw-backup-daily.timer (daily configs)
- openclaw-backup-weekly.timer (weekly models/data)
- token-usage-tracker.timer (15min interval)
- blofin-stack ingestor + paper_engine
- blofin-moonshot-v2 orchestration (cycles on-demand)
- sp500-ingestor, sp500-dashboard (port 8897)
- NQ pipeline services

**Known failed services (non-critical):**
- auto-code-backup-sync.service
- blofin-stack-gapfill.service
- nest-keepalive.service
- nq-postmerge-check.service
- openclaw-full-restore-backup.service

---

## Git Backup Hygiene

Auto-committed NQ-Trading-PIPELINE 4 times during heartbeats:
- Mar 25 06:56 (commit 735bd43)
- Mar 25 08:56 (commit d5dfb7c)
- Mar 25 10:51 (commit 1a06e98)
- Mar 25 12:52 (commit 75ed987)

All repos clean as of last heartbeat.
