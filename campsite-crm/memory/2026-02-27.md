# 2026-02-27 — Session Notes

## Jarvis Home Energy App (jarvis-home-energy, port 8793)

### Left Rail Nav — IMPLEMENTED (late afternoon)
- Replaced fixed bottom nav (8 buttons, 56px) with 72px left rail + 160px sub-rail
- Rail items: Energy, Pool, Thermo, Irrigation (💧 separate!), Cameras, Appliances, Media, Settings
- Sprinklers/Irrigation is its OWN rail item — NEVER under Thermo/Climate. Rob was clear about this.
- Sub-rail appears for sections with sub-pages: Energy (Cockpit/Solar/SPAN/Tesla/Cybertruck), Media (Roku)
- Thermo has NO sub-pages — clicks directly to thermostat view
- Backup before refactor: git commit 890a54e, filesystem backup jarvis-home-energy_leftrail_*
- git commit e25f5d1 = current stable left rail state

### Scrolling Fix
- `.view { overflow: visible }` — views expand to content height, don't create own scroll contexts
- `main` is the single scroll container with `overflow-y: auto !important`
- Cockpit view special case: `overflow: hidden` + JS fitCockpit() sets explicit height
- fitCockpit() targets `#view-cockpit` height = window.innerHeight - header.offsetHeight - nav.offsetHeight (now no bottom nav, just header)

### Cockpit Pages — All fit 1280x800 (confirmed with browser screenshots)
- Default page: Microgrid (3-column). Swipe order: Microgrid → Live Power Flow → Trading → Backup
- localStorage key: `jarvis-cockpit-sub-v3` (bumped to bust Rob's old 'live' cached value)
- fitCockpit() runs on load + resize, multiple setTimeout passes
- Panel DOM order: csub-microgrid, csub-live, csub-trading, csub-backup
- JS array: `_cockpitSubs = ['microgrid', 'live', 'trading', 'backup']`

### Live Power Flow Page Fixes
- viewBox changed from "0 30 700 370" → "0 15 700 430" (POOL node was clipped at y=426)
- Battery card → Cybertruck card (id: ct-lm-badge, ct-lm-soc, ct-lm-sub)
- Old battery-soe/battery-sub/tesla-badge refs in renderState made null-safe (guarded with if(el))

### Ring — FULLY DISABLED
- Was firing 2FA SMS to Rob every 10 min all night (confirmed in logs Feb 25-27, 00:00-03:00)
- Root cause: async_fetch_token() called with email/password when no token cached → Ring sends 2FA
- Fix: Ring removed from poll_cameras() entirely (not just `if False`), snapshot route returns 204 immediately
- Kanban card: c_7e9ed39dc6fc6_19ca19fc7e3 — proper token-based auth flow (future work)
- config.py has RING_EMAIL/RING_PASSWORD — credentials present but Ring code never runs

### Dynamic Height — fitCockpit() approach
- JS measures header + nav heights dynamically, sets view-cockpit explicit height
- Reason: CSS calc(100dvh - 93px) didn't work (browser computed 763px not 707px despite correct math)
- body { overflow: hidden }, main handles scroll

## Key Facts to Remember
- Jarvis Home is at http://192.168.68.72:8793/ (port 8793)
- Single-file Flask app: jarvis-home-energy/app.py (all HTML/CSS/JS inline, ~8000+ lines)
- Tablet target: 1280x800, touch-first, dark theme
- Rob's priorities: Thermostats, Hot Tub (Pool page), Cameras, Energy Cockpit
- Ring invite to jarvis.is.my.coo@gmail.com pending (14-day window from Feb 23)
