# 2026-02-24 — Daily Log

## NQ Pipeline — Major Progress Overnight

### Phase 2 Full Re-Run (All Filters Corrected)
- Fixed `_CANDIDATE_CFG` overrides for ORB, prev_day, vol_contraction (were inflated to 100 max_trades)
- **Final leaderboard (authoritative):**
  1. momentum: PF 2.91, Sharpe 7.68, Calmar 808, ~9/day
  2. orb: PF 2.55 (after feature engineering), Sharpe 6.62, ~3/day
  3. gap_fill: PF 2.10, Sharpe 5.56, ~5.6/day
  4. vwap_fade: PF 2.08, Sharpe 5.56, ~8.6/day
  5. prev_day: PF 2.03, Sharpe 5.33, ~0.84/day
  6. vol_contraction: PF 1.86, Sharpe 4.70, ~4.1/day
- All 6 pass Lucid gates. Phase 3 Tier 1: momentum + orb. Tier 2: gap_fill + vwap_fade.
- vol_contraction PF improved with stricter filtering (1.78→1.86) — selectivity = real edge
- ETB still inflated (21K trades, uses separate `_CANDIDATE_CFG_ETB`) — exclude from Phase 3
- Results pushed to pipeline/session-trading

### ML Exit God-Model — Shelved
- Re-tested on filter-corrected strategies (momentum, gap_fill, vwap_fade) with threshold sweep 0.5-0.8
- Best threshold = 0.8 (nearly inert — 0.4% of trades exit via ML)
- Key insight: model is fundamentally a trailing stop (pnl_drawdown_from_peak = 73% importance)
- Recommendation: implement ATR trailing stop instead (simpler, same behavior) — card in inbox
- Root issue: test period (Oct 2025–Feb 2026) is losing regime for all strategies regardless of exits

### Feature Engineering — Research Scientist (6 AM)
- Added 3 new features: `rolling_sharpe_20`, `vwap_cross_count`, `range_rank_5d`
- ORB PF 2.494 → 2.55 (+2.4%), Sharpe 6.49 → 6.62
- `range_rank_5d` ranks #17 of 116 features (1.04% importance) — actively contributing
- Lookahead validation clean on all strategies
- Next: apply same features to momentum + vol_contraction (expect +2-4% lift)

### LightGBM for vwap_fade — Promoted
- LightGBM beat XGBoost on 8/10 walk-forward folds → promoted to champion

### 5 New Inbox Cards Created
- c_297a0d8662449 — Cyclical time encoding (hour_sin/hour_cos)
- c_b32755e27bdc7 — ATR trailing stop vs ML exit
- c_ea3cf9bf1b393 — Phase 3 paper trading setup (top 3 strategies)
- Plus feature expansion (done) and LightGBM (done overnight)

## Home Energy Dashboard — Major Additions (Feb 23 evening)

### Animated SVG Power Flow Diagram (Energy Cockpit)
- Replaced emoji flow row with full SVG animated power flow
- Topology: SRP Grid → Tesla Gateway V2 → SPAN Panel + CT Charger → House Load; Solar → SPAN
- Animated flowing dashes, line thickness ∝ watts, speed ∝ power (idle/slow/normal/fast)
- Added `ct_charging_w` and `total_load_w` to `_update_summary()` — bridges SPAN+Tesla gap
- CT charging 25W trickle at time of build; total_load_w = SPAN + CT combined

### Wyze Cameras (3 cameras live)
- Credentials fixed: WYZE_API_KEY = "Fn1phBRix8ifLx0t5f3ktIyVfz2uRBSWdipswwjAiEJ0Z8SIAmVnaCNZLoOL", WYZE_KEY_ID = "ec0dd323-1db4-4e81-8cd4-a4feab256bae"
- Double-login bug fixed in poll_cameras() — Client() auto-logs in, don't call .login() again
- Cached Wyze client globally (_get_wyze_client()) — logs in once, reuses; 5-min backoff on 429
- Camera list polls every 60s (not 5s) — Wyze rate-limits aggressively
- wyze-sdk 2.2.0 has NO snapshot/thumbnail method — cameras show "No live feed" placeholder
- 3 cameras: Front Side Cam (D03F275A9799), Upstairs Cam (2CAA8E813AE9), Downstairs Cam (2CAA8E813B36)

### Nest Thermostats (2 thermostats via Google SDM API)
- Google OAuth flow: client_id=574885630755-q50rqpgonjsr603ccjkgl5ra25mmp97e.apps.googleusercontent.com
- Client secret: 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
- Refresh token: 1//06gPKGWopgVBUCgYIARAAGAYSNwF-L9IrCgnJ43ttjwJ9aok18HwaH_QSG1GxA-hDYo1g5JOxjIRcGO3lAcFpDnOfRCnOggM0_RU
- Project ID: edc12ede-0076-42d4-86d8-c87f49aec4b4
- All saved in jarvis-home-energy/config.py
- SDM API: poll_nest() uses _get_nest_access_token() (auto-refresh every hour)
- 2 thermostats: Downstairs (70°F, COOLING) + Loft upstairs (72.4°F, COOLING)
- renderNest() rebuilt to dynamically render 1 card per thermostat with ± setpoint controls
- setpoint API: POST /api/nest/setpoint {device_name, cool_f, heat_f} → SDM executeCommand

### Ring — Pending
- Rob added jarvis.is.my.coo@gmail.com as Ring guest (invite email arrived 9:50 PM Feb 23)
- 14-day window to accept — needs browser to accept at ring.com
- RING_EMAIL/RING_PASSWORD still empty in config.py

## Infrastructure Notes
- blofin-stack: 12 commits ahead of remote + 3 uncommitted files — needs push
- master-dashboard: 2 commits ahead of remote
- ai-workshop: on sports-betting/phase2-real-apps branch (not main)
- Numerai daily bot: fires 6:32 AM MST, last run Feb 23 06:35 success
- CPU spike to 85°C at 6:07 AM — transient (dropped to 57°C within 2 min), alerted Rob via Telegram
- Token usage: 7-day ~55% as of 6:30 AM Feb 24

## Lessons
- **Kanban discipline**: spawn builder → PATCH to In Progress → update status.json must happen together — Rob called this out explicitly
- **Don't sit on experiments**: when conditions are met (profitable strategies), dispatch immediately — Rob called out delay on ML exit re-run
- **ML exit = trailing stop in disguise**: pnl_drawdown_from_peak dominates at 73% — implement ATR trail instead
- **Filter inflation pattern**: any strategy with `max_trades_per_session=100` in _CANDIDATE_CFG is likely inflated — check all new strategies

---

## Afternoon Session (3:31 PM MST) — Pre-Compaction Flush

### NQ Phase 3 — Live Data Feed (BLOCKED)

**IBKR Paper Account Setup:**
- Paper account: DUH860616, port 4002, username: robbyrobaz
- Creds: `/home/rob/.ibkr/config.env` (chmod 600, never in git)
- IB Gateway 10.37 + IBC 3.23.0 installed at `~/ibgateway/` and `~/ibc/`
- Symlink: `~/Jts/ibgateway/1037 → ~/ibgateway` (required for IBC path resolution)
- `ibgateway.service` systemd unit active, IBC auto-login to DUH860616
- `ibgateway-restart.timer` fires daily at 10:00 UTC (3 AM MST)
- `ReadOnlyApi=no` added to `/home/rob/ibc/config.ini` (eliminated Error 321)
- `tzdata` Python package installed (fixed `ZoneInfoNotFoundError: US/Central`)

**Data Feed Status:**
- `pipeline/data_feed/ib_feed.py` built with front-month auto-selection, 150s stale watchdog
- `nq-bar-feed.service` created and enabled
- Historical snapshots work: 1,376 bars retrievable via `reqHistoricalData keepUpToDate=False`
- **BLOCKED**: `keepUpToDate=True` delivers zero bar events — Error 354 (real-time CME NQ data not subscribed on paper account)
- **Options**: (A) Fund IBKR to $500 → activates real-time data; (B) 30s polling fallback with keepUpToDate=False (~15-min delay)
- Confirmed working test: `python3 -c "from ib_insync import IB, Future; ib=IB(); ib.connect('127.0.0.1',4002,clientId=99); bars=ib.reqHistoricalData(Future('NQ','202603','CME'),endDateTime='',durationStr='1 D',barSizeSetting='1 min',whatToShow='TRADES',useRTH=False,keepUpToDate=False); print(len(bars), bars[-1])"`

**Front-month NQ contract:** `NQH6`, conId=730283097, expires 2026-03-20, rolls 2026-03-13

### Phase 3 Kanban Cards
- Data feed: `c_cce423409d8d5_19c913d6663` (In Progress)
- Signal engine: `c_c3d77fca1f6cd_19c913d6683` (Inbox)
- Live trades dashboard: `c_78be8cef1baf9_19c913d669c` (Inbox)

### TradersPost / Execution Chain
- **Webhook URL NEEDS ROTATION** — old UUID `51e37934-7a18-4e37-9dc5-33416a36d579` was exposed in chat
- Execution chain: TradersPost → Tradovate → Lucid prop accounts
- ALL webhooks must include `signalPrice` (Tradovate provides no market data to TradersPost)
- Signal format: POST JSON `{ticker, action (buy/sell/exit), signalPrice, orderType, stopLoss: {type, amount/stopPrice}, takeProfit: {amount/limitPrice}, quantity}`

### BloFin Pipeline
- Phase connections fixed (commit 30a52da): BT window 7→30 days, min trades 50→100, min PF 1.2→1.35
- FT promotion root bug fixed — Phase 4 `ft_started` filter was counting ALL historical trades on re-promotion
- cross_asset_correlation at 140/200 FT trades — nearing FT winner override threshold
- Pending: FT_MAX_MDD 50→75% in gate_ranker.py, volatility_expansion_volume_breakout tier promotion check

### Numerai
- Round 1209 submitted successfully (all 3 models)
- Retrained: val_corr 0.0278→0.0294, Sharpe 1.76→1.90
- robbyrob3 models created: `/home/rob/.openclaw/workspace/numerai-tournament/models_robbyrob3/`
- Race condition noted: robbyrobml `--daily` deletes `v5.1/live.parquet` mid-run for robbyrob2 — needs file locking

### Home Energy Dashboard Fixes
- SPAN circuits parsing bug fixed: 21 circuits now showing correctly
- Both Nest thermostats live: Downstairs 70.9°F, Loft 72.4°F
- Animated SVG power flow diagram added
- Wyze snapshot bug: wyze-sdk 2.2.0 has NO snapshot method — cameras show status only
- Ring integration still pending (browser timed out accepting invite)

### Infrastructure / Ops
- Claw-Kanban at port 8787
- Agent files: `.claude/agents/` — ml-engineer, dashboard-builder, devops-engineer, qa-sentinel, crypto-researcher
- Dashboard URLs: Master :8890 | BloFin :8888 | NQ Pipeline :8891 | Home Energy :8793 | Kanban :8787
- campsite-crm: missing git remote — flagged, not yet actioned
- Token usage: ~56% of 7-day budget, resets March 1 4:00 PM MST
- Sonnet switched to main session model (Opus was 78% of token cost)

### Open Issues / Next Steps
1. Rob decides: fund IBKR $500 OR implement 30s poll_feed.py fallback for Phase 3 testing
2. After data flows: build signal engine (sonnet builder, kanban c_c3d77fca1f6cd)
3. Rotate TradersPost webhook URL at traderspost.io
4. Add ETB `blacklisted` boolean column to strategy_registry (manual re-patching has failed 3x)
5. Add PnL/Calmar tiebreaker to NQ tournament engine (CatBoost +23-40% PnL blocked by <10% PF delta rule)
6. Accept Ring invite via jarvis.is.my.coo@gmail.com (password: is$$sseljs21), set RING_EMAIL/RING_PASSWORD in config.py
7. Tesla Gateway: needs commissioning password from Tesla Energy page (TESLA_PASSWORD empty, 401)
8. Push blofin-stack git (18 modified files ahead of remote)
9. Fix Numerai race condition: file locking on v5.1/live.parquet
10. Apply cyclical time encoding (hour_sin/hour_cos) to ORB after Phase 3 data flows


### TradersPost Webhook Update (3:31 PM)
- Rob confirmed old strategy UUID is fine — not rotating
- **NEW ACTIVE webhook**: `https://webhooks.traderspost.io/trading/webhook/51e37934-7a18-4e37-9dc5-33416a36d579/2ddfa7c41bcf347dc1a599108945b07a`
- Token rotated (new: `2ddfa7c41bcf347dc1a599108945b07a`)
- Rob ran god-model live today via Windows machine → NinjaTrader — it worked
- Server-side pipeline (omen-claw) is next step for always-on execution

