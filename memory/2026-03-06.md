# Memory — 2026-03-06

## TradersPost → Tradovate → Lucid CONFIRMED WORKING

- **Correct ticker**: `MNQ` (not `MNQH6`, not `MNQ1!` — TradersPost resolves front month automatically)
- **Working webhook**: `https://webhooks.traderspost.io/trading/webhook/51e37934-7a18-4e37-9dc5-33416a36d579/222ee493f97b98194432f483f0434b95`
- **Auto Submit**: ON on "Lucid Trading 12 Openclaw" subscription — no approve button
- **Account confirmed**: Lucid Flex 50K `LFE0506429036012`
- **Buy payload**: `{"ticker":"MNQ","action":"buy","orderType":"market","quantity":1,"sentiment":"long"}`
- **Close payload**: `{"ticker":"MNQ","action":"sell","orderType":"market","quantity":1,"sentiment":"flat"}`
- **Full round-trip tested**: open + close, both auto-submitted without human approval
- Old webhook in BOOTSTRAP.md updated to new URL (`222ee493f97b98194432f483f0434b95`)

## ORB Overnight Backtest Results (all complete, committed to NQ-Trading-PIPELINE)

Results dir: `docs/orb_prop_research/`

### Winner: Dual-ORB (5-min + 15-min, both trailing Act=10tk/Trail=5tk)
- PF 6.416, WR 86.3%, 34.6 trades/month
- **$4,772/month EV** on 1× Lucid Flex 50K (4ct)
- **$23,860/month** across 5 accounts

### Trailing stop beats all fixed-TP configs
- Trailing Act=10tk/Trail=5tk: PF 5.467 (5-min alone), PF 6.416 (dual)
- Fixed TP=25tk: PF 0.899 — dramatically worse
- Key insight: trailing captures large moves while locking small profits

### 8am Break Retest — DROPPED
- 20% pass rate on Lucid Flex 50K — too inconsistent
- Do NOT include in ORB signal engine

### Rankings (Lucid Flex 50K EV/month)
1. Dual-ORB 5+15min trailing: **$4,772/mo** (94.4% pass rate)
2. ORB 5-min trailing only: **$4,127/mo** (97.8% pass rate)
3. ORB 15-min trailing only: **$2,988/mo** (91.4% pass rate)
4. ORB 30-min trailing: **$2,906/mo** (96.9% pass rate)
5. 8am combo: $902/mo (30.5% pass rate) — rejected

### Files
- `orb_exit_optimization.md` — TP/trailing sweep results
- `orb_15min_results.md` — 15/30-min ORB windows
- `orb_8am_results.md` — 8am break retest (failed gates)
- `OVERNIGHT_REPORT.md` — executive summary + all rankings

## Overnight Monitoring Infrastructure

- `orb-overnight-check.timer` — systemd timer, fires every 30min
- Script: `brain/status/orb_overnight_check.sh`
- Morning ntfy alert at 7:00 AM MST to topic `nq-pipeline`
- Log: `brain/status/orb_overnight.log`

## Next Action: Build orb_signal_engine.py

Now that optimal config is known, build the signal engine:
- Watches 9:25–9:35 AM ET daily
- ORB = 9:30–9:34 ET (5-min) High/Low
- Entry at 9:35 ET bar close above/below OR High/Low
- 4 contracts, trailing stop (activate 10tk, trail 5tk)
- Also fires 15-min ORB entry at 9:44 ET close
- Posts `{"ticker":"MNQ","action":"buy/sell","orderType":"market","quantity":4,"sentiment":"long/short"}` to confirmed webhook
- Force-exit at 4:00 PM ET (`sentiment: flat`)
- systemd service `nq-orb-signal.service`
- DRY_RUN=True until Rob approves BLE per account

## Stale Codex Cards — Pattern

Several cards throughout the night had `assignee=codex` (Codex is BANNED — out of credits).
- Cards fail immediately with empty logs
- Fix: PATCH `assignee=claude`, re-run → succeed
- Root cause: stale cards created before routing change, NOT new auto-card-generator output
- AUTO_CARD_GENERATOR.md correctly says `assignee=claude` — no config fix needed

## Blofin vwap_reversion Expansion

7 new coins activated for FT: BTC-USDT, XRP-USDT, PYTH-USDT + 4 more
Committed to blofin-stack. Paper trader restarted.

## Moonshot v2 Backtest Running Overnight

- 346% CPU during fold 3 backtest at 4:20 AM — normal, expected
- moonshot-v2.timer (4h cycle) active

## ORB Engine — Bugs Found & Fixed (Morning Session)

### Bug 1: Bar timing (CRITICAL)
- Engine used `bar_minute = now_utc.replace(second=0)` for data lookups
- This looks up the CURRENT (still open) bar — always returns None
- Fix: decouple `current_minute` (timing checks) from `bar_minute = current_minute - 1min` (data lookups)
- Result: engine now reads the last COMPLETED bar correctly

### Bug 2: Trail fires on entry bar (CRITICAL)
- After entering, trail check ran on the same bar used for entry decision
- That bar's H/L includes pre-entry price action (e.g. big wick before our fill)
- Fix: skip trail check when `bar_minute <= entry_bar` — start trail on NEXT bar
- `entry_bar` now stored in state JSON

### Bug 3: TradersPost long-only
- Not a code bug — Rob's TradersPost strategy was configured long-only
- SHORT signal fired correctly at 07:36:05 MST, webhook hit TP, but TP rejected it
- Rob fixed bidirectional setting in TradersPost

### What would have happened (correct engine + TP both-sides)
- OR: 24653.5 / 24732.5 (79pt range)
- Entry: SHORT @ 24651.25 (9:36 ET, 14:35 bar close below OR low)
- 14:37: price dropped to 24616.0 (+35.25pt), trail activated → exit @ 24617.25
- P&L: +34pts = +$68 (1 MNQ) / +$680 (1 NQ) — trade over by 9:37 ET

### Engine config changes
- Ticker changed NQ → MNQ (fee efficiency for testing/scaling)
- 10:30 ET test run configured (OR window shifted 15:30-15:34 UTC) for today's validation
- After test: revert timing back to 14:30 UTC for Monday live run

### State file issue
- Engine only writes `orb_engine_state.json` on events (entry/trail/exit) — not on startup
- Monitoring scripts that check for missing state file = false alarm when pre-market
- Initial state can be written manually to unblock monitors

### ORB monitor cron added
- OpenClaw cron job `f6695631-2732-4d4b-b95d-0340012d5dad` fires every 10min
- Reads engine state/logs/feed, sends Telegram message to Rob
- Auto-disables when state=EXITED or past 21:30 UTC
- Also created systemd `nq-orb-monitor.timer` (ntfy-based backup)

## IBKR Feed Watchdog
- `ibkr-feed-watchdog.timer` (every 30s) checks `last_tick_ts` in status JSON
- Stale >90s → auto-restart feed + ntfy alert to `nq-pipeline`
- Feed was silently disconnected Mar 5 at 23:59 UTC for 6+ hours — watchdog prevents recurrence

## TradersPost Confirmed Working (Both Directions)
- Webhook: `https://webhooks.traderspost.io/trading/webhook/51e37934-7a18-4e37-9dc5-33416a36d579/222ee493f97b98194432f483f0434b95`
- Account: Lucid Flex 50K `LFE0506429036012`
- Ticker: `MNQ` (auto-resolves front month)
- Auto Submit: ON
- Rob fixed long-only → both sides now enabled

---

## ORB ENGINE — FULL SESSION LOG (Mar 6 2026, 9:08 AM MST)

### What was built
- `NQ-Trading-PIPELINE/pipeline/orb_signal_engine.py` — live ORB signal engine
- `nq-orb-signal.service` — systemd service (enabled, running)
- `nq-orb-monitor.timer` — ntfy monitor every 10min (active today)
- `ibkr-feed-watchdog.timer` — feed staleness watchdog every 30s
- OpenClaw cron `f6695631` — AI monitor every 10min (Telegram updates)

### Infrastructure confirmed working
- IBKR feed: `ibkr-nq-feed.service` active, writing `NQ_ibkr_1min.csv` + `nq_feed_status.json`
- Feed watchdog: restarts feed if stale >90s
- TradersPost webhook: `https://webhooks.traderspost.io/trading/webhook/51e37934-7a18-4e37-9dc5-33416a36d579/222ee493f97b98194432f483f0434b95`
- TradersPost account: `LFE0506429036012` (Lucid Flex 50K), Auto Submit ON, **both sides enabled** (Rob fixed long-only)
- Ticker: `MNQ` (TradersPost auto-resolves front month MNQH2026)
- Tradovate execution: confirmed working — Buy + Flat both completed at 10:30 test

### Bugs found and fixed today
1. **Bar timing bug (CRITICAL, fixed)**: engine used `bar_minute = now.replace(second=0)` — looks up the CURRENTLY OPEN bar which doesn't exist yet. Fix: `bar_minute = current_minute - timedelta(minutes=1)` (last completed bar).

2. **Trail fires on entry bar (CRITICAL, fixed twice, now correct)**: after entry fires, the trail check block runs in the same loop iteration on the same bar's H/L (which includes pre-entry price action). Caused immediate exit. Fix: `continue` statement after save_state() in entry block so trail block is skipped on entry iteration.

3. **TradersPost long-only (fixed by Rob)**: strategy was configured long-only so SHORT webhooks were silently rejected. Rob fixed bidirectional in TradersPost settings.

4. **Trail design mismatch (fixed)**: we were sending a single entry with `trailing_stop: 1.25pt` starting immediately. But the BACKTEST design has TWO PHASES:
   - Phase 1: 25pt HARD STOP (pre-activation) — rides out noise
   - Phase 2: when 10 ticks (2.5pt) of profit reached → switch to 5-tick (1.25pt) native trail
   Current engine now implements this correctly.

### Current engine design (as of 9:00 AM MST)
```
File: pipeline/orb_signal_engine.py
Ticker: MNQ | Qty: 1 | DRY_RUN: False
TEST_MODE: True (OR at :30 of each hour for hourly retests)

OR window: XX:30-XX:34 UTC (9:30-9:34 ET in production)
Entry: XX:35-XX:44 UTC bar close above OR_HIGH (long) or below OR_LOW (short)

Phase 1 (entry webhook):
  action: buy/sell + stopLoss: {type: "stop", amount: 25.0}

Phase 2 (activation webhook, fired by code when 10-tick profit reached):
  quantity: 0 (no new position)
  stopLoss: {type: "trailing_stop", trailAmount: 1.25}

Force exit: 21:00 UTC (4 PM ET) — sends exit_all webhook

State file: data/orb_engine_state.json
Log: data/orb_engine.log
```

### Backtest winner (confirmed from orb_exit_optimization.md)
- Config: Act=10tk (2.5pt) / Trail=5tk (1.25pt) / Hard SL=25pt pre-activation
- PF: 5.483 | WR: 92.0% | Trades: 276 | Net PnL: $199,240 (14 months, 4 MNQ)
- Source: `docs/orb_prop_research/orb_exit_optimization.md` Suite 3, rank 1

### What has NOT been tested yet (as of 9:08 AM MST)
1. **Phase 2 activation webhook** — the trail switch from hard SL → native trail has NEVER been tested live. We know the entry webhook works. We don't know if `quantity: 0` + `stopLoss: trailing_stop` correctly modifies the existing Tradovate stop.
2. **Full trade end-to-end with correct trail** — no complete trade has run with the correct two-phase design. Every attempt had a bug.
3. **4-contract scaling** — tested with 1 MNQ only. Monday target is 4 MNQ.
4. **15-min ORB layer** — backtested (PF 3.761), not yet coded into engine.
5. **Multi-account routing** — only `LFE0506429036012` confirmed. 2 other Flex 50K accounts need TradersPost subscription added.

### Hourly test schedule (today)
TEST_MODE=True means OR runs at :30 of current UTC hour.
- 16:30 UTC (9:30 AM MST) — next test — OR builds, entry at 16:35-16:44
- 17:30 UTC (10:30 AM MST) — if still needed
- Engine auto-resets each hour at :00 UTC when hour changes

### What to do if session restarts
1. Check `systemctl --user is-active nq-orb-signal.service` — should be active
2. Check `cat data/orb_engine_state.json` — should show current state
3. Check logs: `journalctl --user -u nq-orb-signal.service --no-pager -n 30`
4. The engine is self-running. Only intervene if it errors or produces wrong behavior.
5. The big unknown: does Phase 2 (activation → trail switch) work in TradersPost?
6. When 10:30 test fires: watch TradersPost trade log for a SECOND order (the trail activation). That second order should appear with a trailing stop.

### Monday production config (to apply after successful test)
- Set TEST_MODE = False in orb_signal_engine.py (OR returns to 14:30-14:34 UTC / 9:30 ET)
- Set QUANTITY = 4 (Rob approved after today's test passes)
- Verify both sides enabled in TradersPost (done)
- Verify Tradovate DLL set to $1,000/day (manually in Tradovate — Lucid doesn't set it)

### Key file paths
- Engine: `/home/rob/.openclaw/workspace/NQ-Trading-PIPELINE/pipeline/orb_signal_engine.py`
- State: `/home/rob/.openclaw/workspace/NQ-Trading-PIPELINE/data/orb_engine_state.json`
- Log: `/home/rob/.openclaw/workspace/NQ-Trading-PIPELINE/data/orb_engine.log`
- Backtest results: `/home/rob/.openclaw/workspace/NQ-Trading-PIPELINE/docs/orb_prop_research/`
- Feed status: `/home/rob/infrastructure/ibkr/data/nq_feed_status.json`
- IBKR CSV: `/home/rob/infrastructure/ibkr/data/NQ_ibkr_1min.csv`

### TradersPost webhook payload reference
Entry (Phase 1):
```json
{"ticker":"MNQ","action":"buy","orderType":"market","quantity":1,
 "sentiment":"long","signalPrice":24865.75,
 "stopLoss":{"type":"stop","amount":25.0}}
```
Trail activation (Phase 2):
```json
{"ticker":"MNQ","action":"buy","orderType":"market","quantity":0,
 "sentiment":"long","signalPrice":24868.5,
 "stopLoss":{"type":"trailing_stop","trailAmount":1.25}}
```
Force flat:
```json
{"ticker":"MNQ","action":"exit_all"}
```

---

## ORB Engine — FULLY WORKING (10:15 AM MST)

### Final confirmed working flow
1. Entry webhook: `{"action":"sell","stopLoss":{"type":"stop","amount":25.0}}` ✅
2. Code monitors bar H/L for 10-tick activation ✅
3. Trail exit webhook: `{"action":"exit"}` — NO sentiment field ✅

### Bugs found and fixed (webhook action reference)
- `exit_all` → INVALID (valid: buy, sell, exit, cancel, add)
- `{"action":"exit","sentiment":"flat"}` → INVALID (sentiment only valid with buy/sell)
- `{"action":"exit"}` → VALID ✅

### Final successful trade (10:07-10:10 MST)
- Entry: SHORT @ 24850.25 | OR: 24862.5/24901.0
- HWM: 24827.5 (+22.75pt) | Trail stop: 24828.75
- Exit confirmed in TradersPost + Tradovate ✅
- P&L: ~+$43 on 1 MNQ

### Monday production config (committed, ac6c0a3)
- TEST_MODE = False (OR at 14:30-14:34 UTC / 9:30 ET)
- OR_OFFSET = 30
- QUANTITY = 1 (scale to 4 after Monday runs clean)
- DRY_RUN = False
- Ticker: MNQ

### Monday checklist
- [ ] Set Tradovate DLL to $1,000/day (manual)
- [ ] Confirm TradersPost both sides ON (done today)
- [ ] Scale to QUANTITY=4 after clean Monday run

---

## Evening Session (18:00+ MST)

### Model Routing Changes
- **Opus enabled for main session** — Rob explicitly stated he's the owner and nothing is "banned by him"
- Updated `~/.openclaw/openclaw.json` → added `anthropic/claude-opus-4-5` to allowed models
- Restarted gateway, confirmed working via `session_status`
- Profit Hunter cron switched to Opus (was Haiku)

### Codex Dispatcher Fix
- Root cause: Auto Card Generator and other crons were creating cards with `assignee=codex`
- Codex is out of credits (until Mar 11)
- Fix: Added PRE-DISPATCH CODEX SWEEP to Jarvis Pulse — patches all `assignee=codex → claude` before every dispatch cycle
- No more failed cards due to Codex rate limits

### Profit Hunter Rewrite
- Old: Found losses and maintenance issues (wrong purpose!)
- New: Find PROFIT opportunities — scan all 3 pipelines for top performers, identify scaling opportunities, quantify monthly EV
- Schedule: Every 12 hours (was every 2 hours)
- Model: Opus
- Focus: "Where's the money we're leaving on the table?" not "What's broken?"

### NQ L2 Scalping — New Project
- GitHub: https://github.com/robbyrobaz/nq-l2-scalping
- Local: `/home/rob/.openclaw/workspace/nq-l2-scalping/`
- Source: YouTube video "3 Order-flow Trading Tricks for Beginners" (o-w5Gxss6T0)
- 3 strategies from video:
  1. Delta Absorption Breakout — absorption (high delta, no price move) precedes breakout
  2. Volume Profile FVG — lowest-volume node in swing range = true FVG for rejection trades
  3. CVD Divergence — CVD vs price mismatch = absorption, trade the passive side
- Pipeline includes `process_video.py` to ingest new YouTube videos → strategy specs → backtest
- Claude Code agent spawned to build full suite against IBKR L2 tick data

### Temperature Alert (12:44-16:20 MST)
- Moonshot-v1 timer was STILL ACTIVE (should have been retired Mar 2)
- Both v1 and v2 running simultaneously → 87-90°C
- Fixed: `systemctl --user stop blofin-moonshot.timer && systemctl --user disable blofin-moonshot.timer`
- v1 timer now disabled; only v2 should run

### Demotion Strategy Discussion (open)
- Rob raised: "Why are we demoting after a few bad FT trades? Nothing is tied to real money yet."
- Current gates too aggressive: NQ demotes at PF<1.1 after 20 trades, Blofin at PF<1.35 after 100 trades
- Proposed: Raise to 200+ trades minimum, only hard-demote at PF<0.8 (clear loser)
- Goal: Keep everything in FT simultaneously — the data collection IS the signal
- Decision pending Rob's input on exact thresholds

### NQ L2 Scalping — Second Video (20:10 MST)
- Source: YouTube video (Pz8f0wWW12M) — comprehensive institutional orderflow guide
- 6 NEW strategies extracted:
  1. **009 Absorption** — High delta but opposite candle close = passive players absorbed aggression
  2. **010 Initiative Auction** — Delta + price aligned with volume = momentum continuation
  3. **011 Exhaustion Reversal** — Decreasing volume on successive pushes = fade the move
  4. **012 LVN Rebalance** — Price returns to low-volume node before continuing trend
  5. **013 Value Area Rejection** — Fade moves to VAH/VAL boundaries (range-bound only)
  6. **014 Failed Auction (Hook)** — Failed breakout of value area = trap, trade reversal
- Agent (amber-bison) timed out after 1h — only completed 009 Absorption (2 trades, PF 0.0, -$8)
- 010 and 011 have folders but no backtest.py; 012-014 not started
- README updated with prominent link to comparison report

### Repo Status
- nq-l2-scalping: https://github.com/robbyrobaz/nq-l2-scalping
- Current: 8 strategies (001-008) fully backtested, 3 partially created (009-011)
- Top config: 001_Aggressive_TP (PF 4.00, Sharpe 11.22, +$24)
- Need to re-run agent to complete strategies 010-014
