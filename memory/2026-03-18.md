# 2026-03-18 — Jarvis Daily Log

## Moonshot Zombie Bug — Root Cause Found & Fixed (overnight 12:35-6:50 AM)

### Root Cause: `TimeoutStopSec=infinity` in live systemd unit
- **File:** `/home/rob/.config/systemd/user/moonshot-v2.service`
- **Bug:** `TimeoutStopSec=infinity` appeared TWICE — systemd sent SIGTERM but waited forever for exit
- **Result:** Hung processes survived 37-38 hours as zombies
- **Fix applied:** Removed both `TimeoutStopSec=infinity` lines, set `TimeoutStopSec=600` (10 min)
- **Verified:** `systemctl --user show moonshot-v2.service -p TimeoutStopUSec` → 10min

### Two Additional Bugs Found (code fixes still needed)
1. **Reddit infinite retry loop** (`social.py ~line 130`) — 429 is IP-wide, code retries every symbol → infinite 5s loop
2. **No wall-clock timeout on `fetch_all_extended`** (`extended.py`) — 470 symbols × 3 batches × 15s = up to 5.9h

### Audit Report
- Full report: `/home/rob/.openclaw/workspace/blofin-moonshot-v2/HANG_AUDIT_REPORT.md`
- Subagent ran on Sonnet, completed in 11m50s

### Key Finding: Social timer does NOT call fetch_all_extended
- `--social` flag only runs `run_social_collection()` (Reddit/RSS/GitHub/CoinGecko)
- The shared log timestamp was a red herring

## Backfill Stuck at 107/469 Symbols (ongoing from Mar 17)
- Crypto agent watchdog restarted backfill 9+ times overnight
- Always stuck at exactly 107/469 symbols
- At restart #9 (4:40 AM), watchdog fixed missing pyarrow dependency
- Status: still needs investigation — may get past 107 now with pyarrow fix

## Blofin Git Issues
- **Index corruption:** Fixed at 12:35 AM (rm .git/index, git reset --mixed HEAD)
- **Push failing:** GitHub auth broken after filter-repo cleanup
  - SSH keys not loaded (ssh-agent not running)
  - HTTPS remote hangs on push (30s timeout)
  - Remote switched to SSH (`git@github.com:robbyrobaz/blofin-trading-pipeline.git`) but SSH denied
  - 19 unpushed commits — all safe locally
  - **Fix needed:** Load SSH keys or reconfigure HTTPS credential caching

## NVIDIA Model Routing
- Session started on `nvidia/llama-3.3-nemotron-super-49b-v1.5`
- After compactions, switched to `nvidia/nemotron-3-super-120b-a12b` (120B model)
- Both running free on NVIDIA cloud
- Rob tested "what model are you?" multiple times — all confirmed NVIDIA models

## Heartbeat 7:48 AM — All Clear
- CPU: 81°C (OK)
- Disk: 42% (184GB used)
- RAM: 22GB available
- Gateway: active
- Numerai models: 15 each (OK)
- Git: NQ repo auto-committed and pushed (5 files)
- 7 failed services (expected old/disabled timers)
- 1 In Progress + 1 Planned kanban card

## Crypto Agent Killed Working Moonshot Cycle (8:09 AM) ⚠️
- Crypto heartbeat cron killed PID 1815622 (4 minutes old, started 08:05)
- **Reason:** Checked stale log file (Mar 16 16:25 timestamp), assumed 39.7h zombie
- **Reality:** New cycle hadn't written to log yet (still in discovery phase)
- Crypto agent self-reported the mistake and is fixing heartbeat logic
- **Lesson for all agents:** Check process start time (`ps -o lstart` / `systemctl show ExecMainStartTimestamp`) BEFORE using log timestamps to determine age
- Next moonshot cycle: 12:05 MST

## Open Items
- Fix blofin-stack GitHub push auth (SSH keys or HTTPS credentials)
- Code fixes for Reddit retry loop and fetch_all_extended timeout (per HANG_AUDIT_REPORT.md)
- Verify backfill gets past symbol 107 with pyarrow fix
- Test NVIDIA cloud models for builder/coding tasks
- NQ entry price tracking implementation
