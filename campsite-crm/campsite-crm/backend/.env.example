# Campsite CRM Backend Configuration

# Server port
PORT=5000

# Environment (development or production)
NODE_ENV=development

# Database path (optional - defaults to ./campsite_crm.db)
# DB_PATH=./campsite_crm.db

# Logging level (optional)
# LOG_LEVEL=info

# ── Gmail OAuth (one-button setup) ───────────────────────────────────────────
# Set these to skip the credential form in the UI — users just click
# "Connect Gmail Account" and get sent straight to Google's consent screen.
# Create credentials at: https://console.cloud.google.com/ → APIs & Services → Credentials
# Add authorized redirect URI: https://your-domain/api/auth/gmail/callback
#
# GOOGLE_CLIENT_ID=xxxxxxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-...
