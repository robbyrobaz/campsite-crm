# 2026-03-16 — Jarvis COO Daily Notes

## Agent Workspace Separation (8:00 PM - 10:00 PM)

### Problem: Context Contamination
- All agents reading from shared `/home/rob/.openclaw/workspace/memory/` — NQ knowledge bleeding into Crypto, etc.
- Workspace-level SOUL.md was corrupted with wrong identity in earlier session
- AGENTS.md files in both agentDir AND workspace had hardcoded shared memory paths

### Architecture Established
Each agent has isolated workspace with own files:
- Jarvis: `~/.openclaw/workspace` (COO-level only)
- NQ: `~/.openclaw/workspace-nq`
- Crypto: `~/.openclaw/workspace-crypto`
- Church: `~/.openclaw/workspace-church`

### Fixes Applied
1. **SOUL.md** — Removed BLE greeting, domain-specific boundaries (NQ agent's domain)
2. **HEARTBEAT.md** — Stripped domain checks, added "What Jarvis Does NOT Check" section
3. **MEMORY.md** — Trimmed 19K → 4K chars (moved domain knowledge to agents)
4. **AGENTS.md** — Trimmed 9.6K → 5K chars (removed stale cron table, domain details)
5. **Daily memory paths** — Changed all 3 agents from hardcoded shared path to relative `memory/YYYY-MM-DD.md`
6. **CRITICAL:** Had to fix BOTH agentDir AND workspace copies (workspace copy is what gets loaded as Project Context)
7. **Kanban removed from domain agents** — they use native `sessions_spawn` for builders now
8. **DISPATCHER.md symlinks removed** from crypto/nq agentDirs
9. **Created daily memory files** in each agent's own workspace with today's domain-specific work

### Key Discovery
OpenClaw's `session-memory` hook already uses `resolveAgentWorkspaceDir(cfg, agentId)` — writes to correct workspace automatically. The contamination was from hardcoded paths in AGENTS.md overriding correct behavior.

### Bootstrap Size Reduction
- Before: ~47K chars total
- After: ~28K chars total (42% reduction)
- MEMORY.md: 19K → 4K (−78%)
- AGENTS.md: 9.6K → 5K (−48%)

## Rob's Directives
- Domain agents don't use kanban — use `sessions_spawn` for builders (native OpenClaw)
- Each agent owns its own daily memory in its own workspace
- Jarvis keeps COO-level stuff only (server health, agent status, cross-cutting)
