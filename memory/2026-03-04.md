# Daily Log — 2026-03-04 (Mar 4)

## Major Fixes

### 🔧 Ingestor CPU Leak — FIXED
- **Problem**: Climbed from 10% to 80%+ within 10-20 minutes every restart, required 5+ manual restarts overnight
- **Root cause**: File I/O on every tick (line 457-458 of ingestor.py) — each tick triggered disk write + fsync overhead. Linear CPU growth as tick count accumulated.
- **Fix**: JSON write buffering to reduce I/O overhead (commit `eeeaf0a`)
- **Status**: Stable now. Ingestor at 6+ minutes uptime holding 8-10% CPU (was 40-90% before)
- **Impact**: Solved cascading temperature issues (87°C → 76-78°C during backtest)

## NQ Pipeline Progress

### 9 Strategies in Live Forward Test
- `zscore_vwap_mr` (PF=8.74, 89.7% WR, 26k trades) — wired, training on 257 features
- `equal_tops_bottoms` (PF=3.02, Sharpe=7.97, 2847 trades) — registry was wiped by bad re-run, restored + re-enabled + wired
- `opening_momentum_bias` (gate=pass, PF=3.27, 78k trades) — trained and wired
- `vwap_reclaim` (PF=1.73) — fixed pkl naming, wired
- + orb, momentum, prev_day, vwap_fade, vol_contraction, inside_bar_breakout (6 original)
- ETB now enabled in god_model_config.json (`"enabled": true`)

### Feature Versioning System — 3-Layer Complete
1. **Layer 1**: `FEATURE_VERSION` hash in ml/features.py changes when features change (currently `09e770a94935`)
2. **Layer 2**: Stale model detection in smb_watcher.py on startup, warnings to smb_status.json, amber indicator in v3 dashboard
3. **Layer 3**: `scripts/validate_all_models.py` validation script (exit 0 for PASS/WARN, exit 1 for FAIL)
- 24h reminder timer fires Wed Mar 4 22:10 MST to check feature versioning health

### New Features + Session Restructuring
- **PreNY session** (08:30-09:30 ET): split from LondonNY. Single universally-recognized boundary (CPI, NFP, jobless claims print here).
- **1h confirmation features**: rsi_1h, macd_hist_1h, ema_align_1h, rel_vol_1h, trend_bars_1h, price_vs_ema8/21_1h, ema8_slope_1h, atr_ratio_1h
- **Retrain completed**: All 6 active models trained with new 257-feature set. FEATURE_VERSION stamped in all pkls.
- Named sessions now 9 total (added PreNY)

### 5 New Strategies Created + Backtesting
- `micro_pullback`: 2-4 bar counter-trend pullback entry in trending market
- `failed_breakout`: Stop hunt reversal — price sweeps level then snaps back
- `atr_breakout`: Volatility expansion after compression, directional close
- `vwap_stretch`: Fade price at VWAP ±2σ band, target ±1σ (distinct from zscore_vwap_mr)
- `session_open_drive`: Trade resumption after first 5-bar open drive
- Status: run_phase2.py running tournament backtests (~36min in), registry entries pending

### NQ Pipeline v3 Dashboard Enhancement
- Added `BT T/Day` (bt_trades ÷ 252) and `FT T/Day` (actual trades ÷ actual calendar span) columns to Strategy Library
- Helps quickly identify high-frequency vs. sparse strategies

### New Research Strategy — atr_exhaustion
- From hourly research cron (Strategy Researcher task)
- WR=90.6%, PF=1.8+, 2,671 events
- Card queued in Planned for full backtest

## System Health

### Blofin Stack
- **Moonshot v1** disabled (Feb 26 migration)
- **Moonshot v2** live and running 4h cycles. Timers were dead (>26h downtime), restarted at 23:25 MST. Last cycle failed due to SQLite database locked (transient concurrency), will retry next 4h window.
- **Paper trading**: 86K+ trades accumulated
- **Critical alert monitor**: no alerts active

### Backup System
- `openclaw-full-restore-backup.service` failed at 01:00 due to GitHub HTTP 408 timeout (network transient, not code issue). Will retry next cycle.

### Numerai Tournament
- 3 models submitting daily via `numerai-daily-bot.service` (timer: 06:32 UTC)
- Note: models_elite and models_elite_robbyrob2 directories are currently empty (0 model files), but pipeline handles gracefully by marking them "unavailable" and still succeeds. models_robbyrob3 has 15 models. Investigate if this is expected or if training is broken.

## Learnings & Gotchas

### Kanban Runner False Failures
- Runner bug: marks cards `Failed` in UI even when builder completes successfully (subtype=`"success"`)
- Pattern appeared 5+ times this session
- Workaround: Check log subtype, mark Done manually if success=true
- **Root**: Kanban runner itself exits non-zero but the actual work succeeded
- Should be fixed upstream in kanban-runner or runner protocol

### ETB (equal_tops_bottoms) Two-Level Blocking
- Strategy was blocked at TWO levels:
  1. Registry: `gate_status=fail` (from corrupted re-run)
  2. Config: god_model_config.json `"enabled": false`
- Had to fix both. This belt-and-suspenders approach prevented accidental reactivation.

### Feature Versioning & Model Retraining
- All existing pkls before retrain showed feature_version=unknown (not stamped)
- WARNs are expected in validator until retrain completes
- Retrain automatically stamps feature_version into pkls
- This is the intended workflow: backtest → add features → retrain → version bump

### Blofin Research Cron
- Hourly research bot runs. This cycle: atr_exhaustion passed (2671 events, high WR). Blofin produced no high-conviction signal (low_vol_breakout 37% WR fail, consecutive_red_bounce inconclusive).

## Git Repo Status
- All major repos committed and pushed (NQ-Trading-PIPELINE, blofin-stack, blofin-moonshot-v2, master-dashboard)
- Auto-commit cron pushes dirty repos before session compaction

## Temperature Observations
- Started session: 90°C (backtest load + ingestor leak)
- After ingestor fix: 57-83°C depending on load (normal for active backtesting)
- Ingestor leak was the cascading root cause of high temps overnight

## Process Audit (03:45 MST)
- **All key services running clean**: gateway, ingestor, paper, watcher, all dashboards
- **No duplicate processes, no zombies/defunct processes**
- **ingestor**: 17min uptime, 4% CPU, 303MB RSS (stable post-leak-fix)
- **nq-smb-watcher**: 77min uptime, 6% CPU, 659MB RSS (healthy)
- **blofin-stack-paper**: 17min uptime, active
- **Transient failures** (will self-heal via timers):
  - moonshot-v2: Failed 03:11 with SQLite "database locked" — retries 04:05 MST (19 min)
  - backup: Failed 01:00 with GitHub HTTP 408 — retries 04:24 MST (38 min)
- **Conclusion**: No manual intervention needed. All processes clean.
