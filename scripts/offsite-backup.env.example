# Copy this file to offsite-backup.env and fill in values.
# Keep secrets and private host info out of git.

# Local source (default is repo/knowledge-base)
# KB_SOURCE_DIR=/home/rob/.openclaw/workspace/knowledge-base

# Remote SSH endpoint
OFFSITE_SSH_USER=backup-user
OFFSITE_SSH_HOST=backup.example.com
OFFSITE_SSH_PORT=22

# Optional: key and host verification pinning
# OFFSITE_SSH_KEY=/home/rob/.ssh/id_ed25519
# OFFSITE_KNOWN_HOSTS=/home/rob/.ssh/known_hosts

# Required: destination base path on remote host
# Final path becomes: <OFFSITE_DEST_BASE>/<OFFSITE_BACKUP_LABEL>/
OFFSITE_DEST_BASE=/srv/offsite/openclaw

# Optional: label for this machine (defaults to hostname -s)
OFFSITE_BACKUP_LABEL=rob-surface-laptop-4
